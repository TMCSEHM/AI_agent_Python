# AI Agent with calls to Google's Gemini

It is set to only perform work within the given working directory set in config.py. Without this restriction, the LLM might run amok anywhere on the machine, reading sensitive files or overwriting important data. 

**This tool is purely experimental DO NOT run "as-is"**
